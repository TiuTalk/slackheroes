<% content_for :header do %>
  <%= render 'deck', deck: ChampionsDeck.new(team: @team) %>
<% end %>

<% @decks.first(5).each do |deck| %>
  <%= render 'deck', deck: deck %>
<% end %>

<%= render 'deck', deck: TeamDeck.new(team: @team) %>
